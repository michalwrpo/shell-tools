# mls
## Version 1.0.0
*Made by Michał Waluś*

Simple version of `ls`. Accepts multiple files as input, will print contents of directories and write names of other files.

Usage:
```
./mls [OPTIONS]... [FILES]...
```

Current version accepts the following flags:
- `-1` - prints only one file per line.
