# mmkdir
## Version 1.0.1
*Made by Michał Waluś*

Simple version of `mkdir`. Creates directories at given input paths. Accepts multiple files as input.

Usage:
```
./mmkdir [OPTIONS]... [PATH]...
```

Current version accepts the following flags:
- `-1` - prints only one file per line.
